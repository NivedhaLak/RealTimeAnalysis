# High Throughput Kafka Processing System

## Designed and implemented a scalable Kafka-based system capable of processing 10 million messages efficiently using async Consumers and Optimized Batching.
 - When the initial a request comes from client, hits the /stockValue api.
 - Which then generates the data and pushes it to Kafka(so far able to process around 10M record)